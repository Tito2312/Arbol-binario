[phases.setup]
nixPkgs = ["jdk"]

[phases.build]
cmds = ["mkdir -p out", "javac -d out src/Nodo.java src/ArbolBinario.java src/Servidor.java src/Main.java", "find out -name '*.class'"]

[start]
cmd = "java -cp /app/out Main"